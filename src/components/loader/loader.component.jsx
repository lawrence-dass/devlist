import React from 'react';

import { LoaderContainer, LoadingText } from './loader.styles';

const Loader = () => (
  <LoaderContainer>
    <LoadingText> Loading... </LoadingText>
  </LoaderContainer>
);

export default Loader;
